#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - checks if value a digit 0-9
 *
 * @c: int to be checked
 *
 * Return: 1 if c is a digit, otherwise 0
 **/
	int _isdigit(int c)
{
	return (isdigit(c));
}

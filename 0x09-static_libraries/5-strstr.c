#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strstr - prints a string, followed by a new line.
 * @haystack: array of chars
 * @needle: array of chars
 * Description: prints sum of both diagonals
 * Return: nothing
 **/
	char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}

#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - computes absolute value
 * @n: int to compute as absolute
 * Return: absolute value
 **/

	int _abs(int n)
{
	return (abs(n));
}

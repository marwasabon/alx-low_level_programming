#include "main.h"
#include <string.h>

/**
 * _strlen - derefencing pointers
 * @s: pointer to character.
 * Return: result of swap
 */
	int _strlen(char *s)
{
	int result = strlen(s);

	return (result);
}
